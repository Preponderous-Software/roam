# Direct dependencies only, with lower bounds so pip can resolve wheels for the
# running Python version. The previous file was a frozen `pip freeze` that
# pinned exact (and transitive) versions — notably pygame==2.1.2, which has no
# wheels for Python 3.11+, so the install scripts failed on a modern default
# Python. Transitive dependencies (attrs, pyrsistent, pyparsing, ...) are now
# resolved automatically. This matches the set the CI Tests job installs.

# Runtime
pygame>=2.6.1
Pillow>=10.0.0
jsonschema>=4.17.3
structlog>=25.0.0

# Test
pytest>=7.1.3
pytest-cov>=4.0.0
